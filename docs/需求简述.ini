就是类似于换装备一样的节点，记录所有的节点激活/忽略状态，然后保存为套装，
可以输入int来切换多套方案
这样就可以在一个工作流里面实现多功能
只需要用切换节点就可以进行套装切换
comfyui自带的控制组节点在应用界面使用不了，而且那个节点只能控制群组，意味着要把所有的可变节点放在一个群组里
比起控制组，直接控制所有节点，会来的更方便
将节点省略到某一个状态，按下记录键就可以保存套装
然后用切换节点输出int就可以进行切换了
然后就是现在的切换节点，最大只支持十个切换，而且输入口不固定，较的麻烦